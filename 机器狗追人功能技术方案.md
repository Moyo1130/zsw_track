# 机器狗巡检中断追人功能技术方案

## 1. 背景与目标

当前系统已经具备以下能力：

- 建图与定位
- 按预设路线巡检
- 通过外部 YOLO `HTTP API Service` 检测 `person`

当前要新增的能力是：

1. 巡检过程中检测到 `person` 后，立即中断巡检，切换到追踪状态。
2. 追踪状态下，机器狗朝向目标并向目标靠近。
3. 若同时出现多个 `person`，只锁定第一个满足锁定条件的目标。
4. 当前锁定目标消失后，退出追踪，恢复巡检并从中断点继续执行。

本次开发由我们自己完成，对方最多提供 YOLO 和部分外部接口支持。

---

## 2. 现状与约束

### 2.1 当前已知可复用能力

- 运动控制链路已经支持 `/cmd_vel`
- 现有导航具备单点导航入口，可用于巡检恢复后的继续执行
- 当前已有巡检路线管理能力
- 当前 YOLO 已可检测 `person`，但还未形成稳定的机器人侧可用控制接口

### 2.2 当前关键约束

- YOLO 不在当前工作区内，通过 `HTTP API Service` 接入
- 当前没有深度信息
- 文档中未确认巡检执行阶段是否已有 pause / resume / progress 接口
- 追踪控制与导航控制不能同时占用运动控制权
- 多目标情况下，首版不能做复杂重识别和目标切换

### 2.3 设计原则

- 尽量少改现有导航、建图、底盘主链
- 新能力优先通过上层状态机整合，不入侵底层控制算法
- YOLO 接口先按外部服务适配，避免与检测端强耦合
- 先实现单目标稳定追踪闭环，再考虑增强功能

---

## 3. 总体方案

### 3.1 总体思路

整体采用“**上层状态机 + 外部检测适配 + 运动控制仲裁**”方案。

新增一个核心模块：`person_follow_manager`

它负责：

- 接收 YOLO 的 `person` 检测结果
- 维护巡检 / 追踪状态机
- 决定何时中断巡检
- 在追踪期间独占运动控制
- 目标丢失后恢复巡检
- 对外发布状态与调试信息

### 3.2 模块划分

建议拆分为以下逻辑模块：

#### A. YOLO 接口适配模块`yolo_format_adapter`

职责：

- 负责调用对方提供的 YOLO HTTP/websocket 服务
- 将 HTTP/websocket 结果转为本地统一检测结果结构
- 屏蔽外部接口变更对主逻辑的影响

#### B. 状态机模块`State_Machine_manager`

职责：

- 接收`yolo_format_adapter`检测结果
- 处理目标锁定、目标保持、目标丢失
- 管理巡检与追踪状态切换
- 生成巡检暂停/恢复命令
- 生成追踪开始/结束命令
- 对外发布当前运行状态，便于 App / 上位机 / 日志系统查看当前阶段

#### C. 巡检上下文适配模块`patrol_context_adapter`

职责：

- 执行开始执行巡检、暂停巡检、恢复巡检、停止巡检等动作
- 获取当前巡检状态、巡检路线与点位进度返回给`State_Machine_manager`

现有巡检执行器没有这些接口，需要我们自己补一个桥接层。

#### D. 跟踪控制模块`Tracking_Manager`

职责：

- 接收`State_Machine_manager`发出的追踪开始/结束命令
- 接收`yolo_format_adapter`的数据用于定位追踪目标
- 接收机器人运动能力接口，实现追踪控制算法，输出追踪阶段运动控制命令

---

## 4. 数据流设计

### 4.1 输入

1. YOLO HTTP/Websocket 服务输出的 `person` 检测结果
2. 当前巡检状态、巡检路线与点位进度
3. 机器人运动能力接口

### 4.2 输出

1. 开始执行/中断/恢复/终止巡检指令
2. 追踪阶段运动控制命令
3. 状态广播、调试日志与追踪诊断信息

### 4.3 主流程

1. 巡检状态下持续轮询或接收 YOLO 检测结果
2. 当检测到 `person` 并满足锁定条件后：
   - 记录当前巡检上下文
   - 暂停当前巡检导航
   - 切换到追踪状态
3. 追踪状态下根据 bbox 位置控制机器人转向和前进
4. 若目标持续存在，继续追踪
5. 若目标丢失超过阈值：
   - 停止追踪运动
   - 清空当前锁定目标
   - 恢复巡检
6. 恢复巡检后重新进入可检测状态

---

## 5. 状态机设计

建议状态机如下：

### 5.1 `PATROL`

含义：

- 正常巡检中
- 持续监听 `person` 检测结果

转出条件：

- 检测到 `person` 且满足锁定条件，进入 `TRACKING_LOCK`

### 5.2 `TRACKING_LOCK`

含义：

- 目标进入锁定确认阶段
- 目的是过滤瞬时误检

转出条件：

- 目标持续满足锁定条件，进入 `TRACKING`
- 目标消失或不满足条件，返回 `PATROL`

### 5.3 `TRACKING`

含义：

- 已锁定一个目标
- 忽略其他 `person`
- 独占追踪控制权

转出条件：

- 目标短时丢失，进入 `TRACKING_LOST`
- 人工接管 / 急停 / 模式切换，直接退出追踪

### 5.4 `TRACKING_LOST`

含义：

- 目标暂时看不到
- 处于丢失缓冲期

转出条件：

- 目标重新出现并与锁定目标匹配，回到 `TRACKING`
- 超过丢失超时时间，进入 `RETURN_TO_PATROL`

### 5.5 `RETURN_TO_PATROL`

含义：

- 结束追踪
- 清理控制权
- 恢复巡检

转出条件：

- 巡检恢复成功后返回 `PATROL`

---

## 6. 目标锁定与目标保持策略

### 6.1 锁定规则

首版规则固定如下：

- 只关注 `class = person`
- 在 `PATROL` 状态下，第一次满足锁定条件的 `person` 作为本次追踪目标
- 锁定后不切换到其他目标

### 6.2 多目标处理

如果同一帧内存在多个 `person``：

- 按对方接口返回顺序处理，选取第一个满足条件的目标
- 如果对方能提供稳定排序规则或目标 ID，优先使用对方提供能力

### 6.3 目标保持

如果对方能提供 `track_id`：

- 直接按 `track_id` 保持目标

如果对方不能提供 `track_id`：

- 我们本地做轻量关联
- 关联依据优先采用：
  - bbox IoU
  - bbox 中心点距离
  - bbox 尺寸变化连续性

### 6.4 目标丢失判定

目标不应“一帧不见就退出追踪”。

建议逻辑：

- 连续若干次请求未命中锁定目标，进入 `TRACKING_LOST`
- 超过丢失超时后，才真正结束追踪并恢复巡检

具体阈值由联调后标定，不在此文档内固化。

---

## 7. 追踪控制策略

### 7.1 基本原则

当前无深度，所以不做目标世界坐标恢复，不做三维定位跟踪。

首版采用纯视觉 2D 闭环控制：

- 用 bbox 中心偏移做转向控制
- 用 bbox 尺寸变化做接近判断

### 7.2 转向控制

控制目标：

- 让 `person` 尽量回到图像中心区域

控制依据：

- `bbox_center_x` 与图像中心的偏差

控制结果：

- 输出角速度控制机器人左右转向

### 7.3 前进控制

控制目标：

- 在目标已经基本居中后再向前推进

控制依据：

- 目标相对中心偏差
- 当前目标 bbox 的尺寸

控制结果：

- 仅在满足前进条件时输出前向速度

### 7.4 停止逼近条件

由于无深度，停止条件由视觉代理量决定。

实现上由我们根据以下信息综合判断：

- bbox 高度占比
- bbox 面积占比
- 连续帧稳定性

阈值需要通过相机安装位、视场角、机器人速度能力进行现场标定。

### 7.5 安全控制

如果现有系统可提供前向避障或安全距离能力，应在追踪阶段同步参与制动判断。

优先级建议如下：

1. 急停 / 人工接管
2. 安全制动
3. 模式切换
4. 追踪控制

---

## 8. 巡检中断与恢复方案

### 8.1 中断时要保存的信息

至少要保存：

- 当前巡检路线名（任务名）
- 当前巡检点列表
- 当前巡检进度点索引

### 8.2 中断动作

检测锁定成功后，系统执行：

1. 暂停巡检（导航）执行器
2. 等待巡检（导航）控制退出运动控制
3. 进入追踪状态并接管控制

### 8.3 恢复动作

目标丢失后，系统执行：

1. 停止追踪控制输出
2. 清除锁定目标
3. 从记录的巡检上下文恢复
4. 恢复原巡检执行逻辑

### 8.4 恢复策略

- 从中断点继续

---

## 9. 对外接口需求与本地适配关系

### 9.1 我们自己负责实现的部分

- 状态机
- 目标锁定逻辑
- 丢失恢复逻辑
- 追踪控制逻辑
- 巡检上下文管理
- 状态广播
- 对外接口适配

### 9.2 对方需要提供或配合的部分

- YOLO HTTP/websocket 接口
- bbox / score / 时间戳 / 图像信息等输出字段
- 如有能力，提供 `track_id`
- 服务稳定性说明、限流说明、异常返回说明

---

## 10. 内部接口建议

以下是我们内部建议统一出来的逻辑接口，不一定一开始全部对外暴露，但内部要按统一结构设计。

### 10.1 检测结果统一结构

建议统一为以下语义：

- `timestamp`
- `image_width`
- `image_height`
- `detections[]`
- 每个 detection 包含：
  - `class_name`
  - `score`
  - `bbox`
  - `track_id(optional)`
