# 云端追人接口调用说明

本文档面向云端系统开发人员，说明如何调用追人状态机 FastAPI 服务。

## 1. 服务关系

```text
云端系统
  -> person_follow_api.py
    -> person_follow_state_machine.py
      -> patrol_api.py
        -> 机器人巡检 WebSocket 接口
      -> track/ 跟踪与机器人运动控制
```

云端系统只直接调用 `person_follow_api.py` 提供的接口。`patrol_api.py` 由追人状态机内部调用，用于巡检暂停、恢复、重建等动作。

## 2. 启动服务

运行依赖：

```powershell
pip install fastapi uvicorn websockets python-dotenv
```

在本目录执行：

```powershell
uvicorn person_follow_api:app --host 0.0.0.0 --port 8001
```

端口约束：如果 YOLO WebSocket 服务已经占用 `8001`，追人 API 服务必须改用其他端口，例如 `8002`。云端调用地址中的端口应同步改为实际 API 服务端口。

服务启动后，云端通过 HTTP 调用：

```text
http://<设备IP>:8001/person-follow/start
http://<设备IP>:8001/person-follow/status
http://<设备IP>:8001/person-follow/stop
```

## 3. 调用顺序

1. 云端或巡检系统先启动正常巡检。
2. 云端调用 `POST /person-follow/start` 启动追人状态机。
3. 云端定期调用 `GET /person-follow/status` 获取状态机状态。
4. 需要人工结束或任务结束时，云端调用 `POST /person-follow/stop`。

## 4. 启动追人

接口：

```text
POST /person-follow/start
Content-Type: application/json
```

请求示例：

```json
{
  "yolo_ws": "ws://10.69.235.139:8001/ws/detection",
  "robot_ip": "10.69.235.139",
  "robot_port": 43893,
  "map_name": "0317_1",
  "route_name": "route_a",
  "dry_run": false,
  "lock_confirm_frames": 5,
  "min_person_score": 0.7,
  "min_person_height_ratio": 0.15,
  "pause_reason": "person_follow_interrupt",
  "rebuild_on_bad_resume": true,
  "yolo_recv_timeout_s": 0.5,
  "max_empty_frames_after_timeout": 60
}
```

参数说明：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|---|---:|---:|---|---|
| `yolo_ws` | string | 是 | 无 | YOLO WebSocket 地址，服务会持续消费该连接的数据。 |
| `robot_ip` | string | 否 | `10.69.235.139` | 机器人运动控制 IP。 |
| `robot_port` | integer | 否 | `43893` | 机器人运动控制 UDP 端口。 |
| `map_name` | string | 否 | `0317_1` | 巡检恢复或重建时使用的地图名。 |
| `route_name` | string/null | 否 | 环境变量 `PATROL_ROUTE_NAME` | 巡检恢复或重建时使用的路线名。 |
| `dry_run` | boolean | 否 | `false` | `true` 表示不向机器人发送运动控制指令，适合联调。 |
| `lock_confirm_frames` | integer | 否 | `5` | 连续检测到有效人员多少帧后进入追踪。 |
| `min_person_score` | number | 否 | `0.7` | 人员检测最低置信度。 |
| `min_person_height_ratio` | number | 否 | `0.15` | 人员检测框高度占画面高度的最低比例。 |
| `pause_reason` | string | 否 | `person_follow_interrupt` | 暂停巡检时写入的原因。 |
| `rebuild_on_bad_resume` | boolean | 否 | `true` | 巡检恢复状态异常时是否重建剩余巡检流程。 |
| `yolo_recv_timeout_s` | number | 否 | `0.5` | 等待 YOLO 下一帧的超时时间，单位秒。 |
| `max_empty_frames_after_timeout` | integer | 否 | `60` | 追踪阶段 YOLO 超时后最多补多少个空帧。 |

成功响应：

```json
{
  "ok": true,
  "message": "person follow started",
  "state": {
    "running": true,
    "params": {},
    "last_snapshot": null,
    "last_error": null,
    "last_event": "starting",
    "started_at": "2026-05-19T16:30:00.000+08:00",
    "stopped_at": null
  }
}
```

重复启动响应：

```json
{
  "detail": "person follow is already running"
}
```

HTTP 状态码为 `409`。

## 5. 查询状态

接口：

```text
GET /person-follow/status
```

请求不需要参数。

成功响应示例：

```json
{
  "ok": true,
  "state": {
    "running": true,
    "params": {
      "yolo_ws": "ws://10.69.235.139:8001/ws/detection",
      "robot_ip": "10.69.235.139",
      "robot_port": 43893,
      "map_name": "0317_1",
      "route_name": "route_a",
      "dry_run": false
    },
    "last_snapshot": {
      "state": "TRACKING",
      "previous_state": "TRACKING_LOCK",
      "locked_track_id": 12,
      "lock_seen_frames": 5,
      "last_error": null,
      "patrol_context": {},
      "tracking": {},
      "detection_debug": {},
      "timing": {}
    },
    "last_error": null,
    "last_event": "yolo_websocket_connected",
    "started_at": "2026-05-19T16:30:00.000+08:00",
    "stopped_at": null
  }
}
```

状态机 `state` 字段可能值：

| 值 | 含义 |
|---|---|
| `PATROL` | 正常巡检，尚未进入追人。 |
| `TRACKING_LOCK` | 已检测到候选人员，正在确认锁定。 |
| `TRACKING` | 已暂停巡检并执行追人。 |
| `TRACKING_LOST` | 目标丢失。 |
| `RETURN_TO_PATROL` | 正在停止追人并恢复巡检。 |
| `ERROR` | 状态机发生错误。 |

## 6. 停止追人

接口：

```text
POST /person-follow/stop
Content-Type: application/json
```

请求示例：

```json
{
  "reason": "manual_stop"
}
```

参数说明：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|---|---:|---:|---|---|
| `reason` | string | 否 | `manual_stop` | 停止原因，会传给状态机 `stop()`。 |

成功响应：

```json
{
  "ok": true,
  "message": "person follow stopped",
  "state": {
    "running": false,
    "params": {},
    "last_snapshot": {},
    "last_error": null,
    "last_event": {
      "event": "stopped",
      "reason": "manual_stop",
      "timestamp_iso": "2026-05-19T16:35:00.000+08:00"
    },
    "started_at": "2026-05-19T16:30:00.000+08:00",
    "stopped_at": "2026-05-19T16:35:00.000+08:00"
  }
}
```

如果服务当前没有追人任务，接口仍返回 `ok=true`，`message` 为 `person follow is not running`。

## 7. 常见错误

| 场景 | 表现 | 处理方式 |
|---|---|---|
| YOLO WebSocket 地址错误 | `last_error` 出现连接失败信息，`running=false` | 检查 `yolo_ws` 地址、端口、token 和 YOLO 服务状态。 |
| 重复启动 | HTTP `409` | 先调用 `/person-follow/status` 判断运行状态，或调用 `/person-follow/stop` 后再启动。 |
| 机器人控制失败 | `last_error` 出现 UDP 或控制器错误 | 检查 `robot_ip`、`robot_port` 和机器人控制链路。 |
| 巡检恢复失败 | `last_snapshot.last_error` 或 `last_error` 出现恢复失败信息 | 检查 `map_name`、`route_name`、巡检任务状态和 `patrol_api.py` 到巡检 WebSocket 的连通性。 |
| YOLO 长时间无检测帧 | `last_event` 出现 `yolo_timeout_empty_frame_limit_reached` | 检查 YOLO 是否仍在推流，或调大 `max_empty_frames_after_timeout`。 |
